## Описание новых инструментов

### Декоратор `log`
В проекте реализован декоратор `@log`, который предназначен для автоматического логирования работы функций.
- **Логирование успеха:** записывает имя функции и статус `ok` при успешном выполнении.
- **Логирование ошибок:** в случае сбоя записывает имя функции, тип возникшей ошибки и входные параметры, с которыми была вызвана функция.
- **Гибкость:** декоратор может выводить логи как в консоль (по умолчанию), так и в файл (если указан аргумент `filename`).
- Я добавил функционал поиска транзакций по описанию с помощью регулярных выражений.
Реализовал удобный интерфейс в main.py для выбора формата файла и фильтрации.
- - Добавлена функция process_bank_search для поиска операций по описанию с использованием регулярных выражений (re).
> - Добавлена функция process_bank_operations для подсчета количества операций по категориям с использованием Counter.
> - Реализован основной интерфейс пользователя в модуле main.py.